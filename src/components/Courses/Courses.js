import React from 'react';

import Layout from '../Layout/Layout';
import CourseItemsAll from '../CourseItems/CourseItemsAll';

const Courses = () => {
  return (
    <Layout>
      <CourseItemsAll />
    </Layout>
  );
};

export default Courses;
